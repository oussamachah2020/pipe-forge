# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DATABASE_PATH=./data/database.sqlite
```

This file shows what environment variables your application needs. Users copy this to `.env` and fill in their own values.

#### `./.dockerignore`
```
